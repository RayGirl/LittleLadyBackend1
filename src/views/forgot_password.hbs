<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LittleLady Password Reset Email</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin-top: 90px;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 70vh;
        }
        .email-container {
            background: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            max-width: 800px;
            width: 100%;
            padding: 20px;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            height: 300px;
        }
        .email-container h1 {
            font-size: 24px;
            color: #333333;
            margin-bottom: 20px;
        }
        .email-container p {
            font-size: 16px;
            color: #555555;
            margin-bottom: 20px;
            line-height: 1.5;
        }
        .email-container a.button {
            display: inline-block;
            background-color: #3E4F22;
            color: #ffffff;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
        }
        .email-container a.button:hover {
            background-color: #F4E9F1;
            color: #040400;
        }
        .email-container .footer {
            font-size: 12px;
            color: #888888;
            margin-top: 70px;
        }
        .email-container .footer a {
            color: #007bff;
            text-decoration: none;
        }
        .email-container .footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <h1>Password Reset</h1>
        <p>Seems like you forgot your password for LittleLady. If this is true, click below to reset your password.</p>
        <a href="{{link}}" class="button">Reset My Password</a>
        <p>If you did not forget your password you can safely ignore this email.</p>
        <div class="footer">
            © 2025 LittleLady, Inc.
        </div>
    </div>
</body>
</html>
